apiVersion: apps/v1
kind: Deployment
metadata:
  name: caliper
  labels:
    app: caliper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: caliper
  template:
    metadata:
      labels:
        app: caliper
    spec:
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: mypvc
        - name: caliper-config
          configMap:
            name: caliper-config
            items:
              - key: caliper-network.yaml
                path: caliper-network.yaml
              - key: config.yaml
                path: config.yaml
      containers:
        - name: caliper
          image: majedur708/hlf-api:500.34
          imagePullPolicy: Always
          env:
            - name: CALIPER_NETWORKCONFIG
              value: /caliper/config/caliper-network.yaml
            - name: CALIPER_BENCHCONFIG
              value: /caliper/config/config.yaml
          volumeMounts:
            - name: data
              mountPath: /organizations
              subPath: organizations
            - name: data
              mountPath: /usr/src/app/connection-profile
              subPath: connection-profile
            - name: caliper-config
              mountPath: /caliper/config/caliper-network.yaml
              subPath: caliper-network.yaml
            - name: caliper-config
              mountPath: /caliper/config/config.yaml
              subPath: config.yaml
          command: ["npx"]
          args:
            - "caliper"
            - "launch"
            - "manager"
            - "--caliper-fabric-gateway-enabled"
            - "--caliper-fabric-gateway-rpcoptions.grpc.keepalive_time_ms=300000"
            - "--caliper-fabric-gateway-rpcoptions.grpc.http2.max_pings_without_data=0"

          resources:
            requests:
              memory: "800Mi"
              cpu: "600m"
            limits:
              memory: "2Gi"
              cpu: "4"
---
apiVersion: v1
kind: Service
metadata:
  name: caliper
  labels:
    app: caliper
spec:
  selector:
    app: caliper
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
# ---------------------+------+------+-----------------+-----------------+-----------------+-----------------+------------------+
# | Name                | Succ | Fail | Send Rate (TPS) | Max Latency (s) | Min Latency (s) | Avg Latency (s) | Throughput (TPS) |
# |---------------------|------|------|-----------------|-----------------|-----------------|-----------------|------------------|
# | createSystemManager | 20   | 0    | 11.4            | 3.02            | 1.26            | 1.91            | 6.5              |
# |---------------------|------|------|-----------------|-----------------|-----------------|-----------------|------------------|
# | getSystemManager    | 1000 | 0    | 63.1            | 1.50            | 0.01            | 0.14            | 63.0             |
# +---------------------+------+------+-----------------+-----------------+-----------------+-----------------+------------------+
